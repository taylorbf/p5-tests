
/* talk about:
   sequence of events (background)
   color rgb()
   again, coordinate grid
   mouseX and mouseY if adventurous --- this might only be like 2 hrs of class
*/

function setup() {
  createCanvas(200,200)
  noStroke()